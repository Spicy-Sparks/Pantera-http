import { PanteraConfig } from '../types';
export declare const mergeConfig: (config1: PanteraConfig, config2: PanteraConfig) => PanteraConfig;
export declare const mergeUrl: (url: string, baseUrl?: string) => string;
